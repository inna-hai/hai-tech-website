{
  "name": "hai-tech-lms-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create lms-database",
    "db:migrate": "wrangler d1 execute lms-database --file=./src/db/schema.sql",
    "db:migrate:local": "wrangler d1 execute lms-database --local --file=./src/db/schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.2.0"
  }
}
